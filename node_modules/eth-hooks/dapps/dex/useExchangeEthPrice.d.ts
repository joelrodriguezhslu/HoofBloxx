import { TNetwork } from "../../models";
import { TEthersProvider } from "../../models/providerTypes";
/**
 * Get the Exchange price of ETH/USD (extrapolated from WETH/DAI)
 * @param targetNetwork
 * @param mainnetProvider
 * @param pollTime
 * @returns
 */
export declare const useExchangeEthPrice: (targetNetwork: TNetwork, mainnetProvider: TEthersProvider, pollTime?: number) => number;
//# sourceMappingURL=useExchangeEthPrice.d.ts.map